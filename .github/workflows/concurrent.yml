name: COncurrent

on:
  workflow_dispatch:  
  push:
    branches:
      - dev
  pull_request:
     branches:
      - dev 

concurrency:
  group: ci_workflow_group
  cancel-in-progress: false

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: Step 1
        run: |
         echo "This is Workflow 1 - Job 1"
        
